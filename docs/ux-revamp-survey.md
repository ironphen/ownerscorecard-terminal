# UX revamp survey — ruling record (2026-07-31)

Mandate: Ryan, 2026-07-31, verbatim: "feel free to totally revampo and improve the UX design, nav, anyhting that you think you can improve" — executed under the standing delegated-ratification grant (canon reasoning, recorded rulings). Survey: four auditors walked the BUILT site (5,801 pages) — navigation/IA, reader journeys, visual system, mobile/performance — every finding measured on the rendered HTML or the components; a hostile skeptic then killed, merged, and ranked. Binding priors carried in: the home-page claim/h1 and the record table's inner design are the owner's (reverted 2026-07-26); progressive disclosure ratified; subtraction favored; articles swept for deep-link anchors before any cut.

## The ruling (the skeptic's synthesis, verbatim)

Verified before ruling: articles cite only `#sec-record/oeq/capalloc/mix/peers/price` (sec-recent and sec-capital uncited — cut-safe); `/ledger` and `/manual` absent from BaseLayout nav; no `/industries` rule in `_redirects`; `graham.mjs:120-124` returns "none paid"+fail on an all-null column despite its own line-111 doctrine; `fetchFundamentals.mjs:208` carries only 3 corporate dividend tags but a corroborated altTag-fill machine already exists at :1690-1717; Peers.astro has zero `@media`; 8 `font-weight:800` declarations against a `400 700` face; overscroll containment exists only in the two groupings files.

RULING — THE SKEPTIC AND THE BUILD ORDER

== KILLED ==

- K1 Type-scale token migration (Visual, 43 sizes, "large"). Half-pixel accretion is real and invisible. A pool-wide migration is maximum regression surface for zero reader value, and "collapse where no deliberate distinction exists" demands a per-declaration judgment call — that is a re-skin of the plain voice by a thousand cuts. Tokens may be declared for NEW components; no campaign.
- K2 Vertical rhythm --sec-gap. 18-26px deltas are imperceptible at reading distance; stripping root margins from 17 components risks new seams on 3,613 pages to cure a non-problem.
- K3 Clamp/lede consolidation. 1px-at-max differences. The one real item (owners h1 26px vs 28px) rides along whenever those files are next touched; not a build item.
- K4 Hiding "Open anywhere" on narrow screens (half of the perf auditor's nav item). Hiding a browse door per-viewport is a cut of function; the door was praised as deliberate. The padding bump survives (item 14). Hamburger refusal ratified.
- K5 Home-page "or read the Manual" fallback. Touches home arrangement beside the owner's claim; the masthead route suffices.
- K6 grp CSS extraction. The auditor already withdrew to "a comment pointing at its twin" — that comment ships as a ride-along, not a build item. Deliberate tonal divergence stands.
- K7 (partial) In-table annotation for the TTM payables basis — that is the record table's inner design, the owner's. The data-side guard survives (item 5).

== MERGES / CONFLICT RULINGS ==

- M-A Wire bare-delta + sec-recent apology (Journeys) = ONE item, one extraction store. sec-recent verified uncited by any article; conditional suppression is cut-safe.
- M-B Ledger / Manual / flags-withheld-tests nav links: distinct findings, one BaseLayout+almanac+about commit.
- M-C secnav mobile fade/tap (perf) + jp/eu retrofit (nav): extract the cbar once, both consumers inherit the fixes. One item.
- M-D KO SBC basis + SBC-paragraph dedup + 150%/112% window naming = one company-page copy pass.
- M-E CSS honesty batch: dead .secHead + h4 demotion + 800→700 + stale --warn fallbacks + .kicker promotion. All zero- or near-zero-visual.
- Conflict ruled: nav auditor's refusal to put Flags in the masthead is RATIFIED — a top-level Flags item is verdict-prominence; arrangement is a pronouncement.
- Conflict ruled: --faint is promoted out of the hygiene batch — a measured AA failure on every page's footer is reader-facing, not tidiness.

== BUILD ORDER == (reader-value per unit risk; smallest first within rank)

RANK 1 — the record must not lie (correctness is the moat)
1. MLP distributions honesty rule. Files: src/lib/graham.mjs (~:113-128: all-null dividendsPaid column → render "no dividend line tagged in the data", withhold pass/fail — line 111 already states the doctrine), Scorecard "Where do the earnings go?" card + TenYear.astro:110 inherit sec-capital's hedge, never "$0 dividends". Accept: built /c/EPD shows no "none paid · Miss" and no "$0 dividends"; zero pages pair "none paid" with the "no dividend line reported" hedge (today 710).
2. Partnership distribution tags. Files: scripts/fetchFundamentals.mjs:208 — add DistributionsMade*/PaymentsOfDistributions* to the EXISTING corroborated altTag-fill (:1690-1717), per-candidate corroboration per the Brixmor ruling (blanket rung refuted). Accept: EPD record shows ~$4.8B FY2025 distributions; the 24 named partnership pages read a real dividend column; fill log names each corroboration.
3. KO copy pass (M-D). Files: Scorecard/Valuation/pay components. Accept: every SBC quote names its basis (FY2025/TTM); sub-1% ratios one rounding; SBC doctrine paragraph once per page; card reads "this year 150% · over the record 112%".
4. Wire/sec-recent one store (M-A). Files: src/lib/wirePerf.mjs, src/pages/c/[ticker].astro:310, wire.astro — fall back to the filingFacts merged quarter (cf0d848); if still empty, suppress the banner (cut-safe). Accept: /c/KO sec-recent carries the delta line its own vital signs already show; apology-banner count 49 → ~0; KO's wire row gains figures.
5. TTM payables pair-guard, data-side only. Files: fundamentals pipeline (utilities reg-pair pattern): combined-concept Q tag → decompose via FY relationship or withhold the TTM cell AND its derivatives (working capital, current ratio). Accept: KO TTM working capital no longer swings $1.8B→($7.1B) on a tag basis; withheld cells use the existing em-dash idiom. OWNER-ASK only if in-table annotation is ever preferred (K7).

RANK 2 — reachability (one-line wins, 5,801-page inheritance)
6. Ledger → publication cluster, between Notes and Principles (BaseLayout.astro:397-401). Accept: grep dist shows href="/ledger" on every page.
7. Grouping name-cell gets the /c/TICKER anchor (groupings/[slug].astro + sector twin — the completeness-guard lesson: BOTH surfaces). Accept: banks name cell is a link; compare checkbox untouched; click target ~10x.
8. Recent-filing card date → /wire/{recentFiling.date} (c/[ticker].astro:293-313). Accept: /c/ADM card date links the day page beside the EDGAR link.
9. /industries 301 → /groupings in _redirects (verified absent) — ships now; contents page is item 17. Accept: bare /industries no longer 404s.
10. Manual → records cluster after Catalog. Accept: nav renders Manual on built pages; flex-wrap degradation checked at 375px.
11. Flags/withheld/tests inbound: almanac sibling line (/flags, /tests/net-nets), one /withheld line each on /about and /docs/data beside their ledger links. Accept: external inbound counts 0→≥1 (/flags), 1→≥3 (/withheld). Masthead Flags stays refused.

RANK 3 — mobile mechanics (small, measured)
12. overscroll-behavior-x: contain on TenYear.astro:488, Peers.astro:280, OwnerEarningsBridge.astro:208, Valuation.astro:796 (rationale already written at groupings/[slug].astro:344). Accept: property present in built CSS for all four.
13. iOS focus-zoom: catalog.astro:140 and owners/index.astro:120 inputs → 16px (or ≤700px override). Accept: no input under 16px in built CSS.
14. Touch targets inside the existing pointer:coarse pattern: chip X ≥28px (destructive), Follow/copyLink/cmpGo/azbar ≥40px hit area via padding/::before; masthead links 8px vertical padding at ≤520px. Accept: computed hit heights at 375px; visual size unchanged on desktop.
15. --faint reclassification: footer small, .dl/.dn, .rsub, .chipLbl, .ct → --muted; --faint reserved for glyphs/separators/absent-cell em-dash. Accept: footer contrast ≥4.5:1 both themes (script-measured); td.num.off unchanged.
16. secnav shared component (M-C): extract cbar/secnav from c/[ticker].astro:268-500 behavior-identical, add TenYear's right-edge fade + coarse tap height ~34px, mount on /jp and /eu with their act anchors. Accept: /c/ desktop pixel-identical; at 375px the clipped Price tab is signalled; jp/eu built pages carry the sticky bar. EU chapter chips deferred until EU classification exists.
17. Peers narrow handling: sticky-left company th with opaque bg + seam shadow (groupings pattern), TenYear's 560px font step. No column cut, nothing named. Accept: mid-scroll at 375px every row keeps its identity cell.

RANK 4 — the book
18. /industries contents page (101 chapters under 11 sector heads — data groupings/index already renders, framed as chapters) + each chapter header links its sector + prev/next chapter feet (indFoot pattern). Accept: /industries lists 101; /industries/railroads links /groupings/sector/industrials and two neighbors; item 9's 301 replaced by the page.

RANK 5 — weight and hygiene (zero reader-visible change permitted)
19. Scope-attr fix on the five hot list pages only (is:global keyed under a page-root class) + drop catalog data-s (filter lowercases textContent once). Accept: catalog raw well under 1.18MB→~700KB, filter works, DOM visual diff nil; company pages untouched.
20. Dead bundles: remove the orphaned island import; client.JUmeKr2y.js (186KB) and CompoundingExplorer absent from next build.
21. CSS honesty batch (M-E): delete TenYear.astro:482 dead .secHead; scorecard/moat subheads h3→h4; unify/comment the two live .secHead rules; 8× 800→700 (what already renders, made honest); drop 8 stale --warn fallbacks; promote .kicker (11px/700/0.09em) global + fold the 7 duplicate definitions and exact 0.08em drifts only. Accept: visual diff on /c/KO and /flags imperceptible; heading census 1×h1/4×h2/13×h3/7×h4.
22. Wire results-8-K expected line, "figures arrive with the 10-Q · expected M/D–M/D", from the existing week-ahead model, statutory due-by fallback, only where a window exists. Last because it is an addition and judgment-confidence. Accept: no press-release parsing introduced; grave styling and day order unchanged.

== OWNER-ASK (not built) ==
- Nav relabel "Owners"/"Statement" → "Meetings"/"My statement": measured ambiguity, but the words are naming, and naming is his.
- Any annotation rendered INSIDE the record table (TTM basis footnote) — inner design is his; item 5 ships the withhold instead.
- Item 2's fill touches Graham-test verdicts on 24 tickers at once — ship under the standing grant's canon-reasoning delegation, but enter it in the ruling record with the corroboration log attached.

== PRESERVE (no build may break) ==
- Record table inner design entire: pinned-right open, left-edge fade, sticky-right labels, 560px short-label step. It is also the pattern SOURCE for items 16-17.
- cbar/secnav behavior: act tabs, IntersectionObserver spy, self-pruning empty-section links, auto-scroll of active tab — extraction (16) must be behavior-identical on /c/.
- Masthead as clustered publication sections; "/" search; Open anywhere on every viewport; no hamburger, no flattening.
- Four-act Graham order with price last and reader-typed price; the details.why collapse discipline (~1,684 visible of 2,631 raw words).
- Groupings doctrine: zero tone color in tables ("no row or cell is ever highlighted"), frozen panes with written rationale, sort-order in the URL, as-filed foreign currency sorting to the back, 62vh pane, pointer:coarse targets.
- The honesty apparatus verbatim: ledger, /withheld, "named, never passed off as current", the no-estimate week-ahead line, BSVN's "Next report" sentence.
- _redirects link-equity discipline and the pre-cut article grep (re-verified this audit: cited anchors are sec-record/oeq/capalloc/mix/peers/price only — sec-capalloc stays forever).
- Semantic color tokens twice-defined + print force-light; dark-default pre-paint script; tone semantics resolving to one --good/--bad/--warn everywhere they are allowed.
- Manual/Industry dual prev/next pure-arithmetic ordering; wire day-page backward chaining; grave-8-K styling.
- Company-page weight (137KB median, no framework runtime) — items 19-20 must not touch company-page architecture.

## Auditor findings (the evidence base)

### Navigation and information architecture

Navigation and information architecture — walked the built site (dist/client, 5,801 pages) as a first-time reader: home, masthead/footer (src/layouts/BaseLayout.astro), and built pages of every type (/c/ADM, /c/AAPL, /groupings/railroads, /groupings/sector/industrials, /groupings index, /industries/railroads, /almanac, /wire, /wire/2026-07-30, /ledger, /owner, /owners, /manual, /catalog, /compare, /flags, /tests, /withheld, /notes). Headline counts: the global masthead reaches 10 destinations from every page, but of the seven core surfaces (wire, groupings, almanac, ledger, owner, manual, compare) only 5 are one click from home and from a grouping page, 6 from a company page — the Ledger is one click from nowhere (5 inbound pages of 5,801), the Manual has zero links from home or nav, /industries has no index page and the bare URL 404s, and /flags has zero external inbound links. The company page's sticky section bar (cbar/secnav) is real, covers all 17 sections through 4 scroll-spied act tabs, and exists only on /c/ pages — not on the 204 /jp or 45 /eu pages.

- **Masthead nav (src/layouts/BaseLayout.astro:397-401, publication cluster) — the Ledger** [small, measured]
  Problem: The publication's accountability register is unreachable from the navigation and from 5,796 of 5,801 pages. A reader on any company, grouping, wire, or the home page cannot discover that the site keeps a verbatim ledger of its own dated conclusions — the single strongest credibility surface the product has.
  Evidence: grep over dist/client: href="/ledger" appears on exactly 5 external pages (notes/index.html, notes/the-moat-and-the-multiple, notes/weeds-and-flowers, about, docs/data) plus itself. The home page, all 3,613 /c pages, all 101 groupings, and the wire carry zero ledger links. The only reader path is Home -> Notes -> the one-line "ledgerLine" under the Notes h1 ("Notes that reach a dated conclusion are entered, verbatim, in <a href=/ledger>"). The nav's own comment (BaseLayout.astro:246-249) describes the second cluster as "the publication (wire/notes/principles)" and says "New areas join as new clusters."
  Proposal: One line in BaseLayout: add <a href="/ledger">Ledger</a> to the publication cluster, between Notes and Principles. The ledger is part of the Notes apparatus, so it joins an existing cluster rather than adding a new one. No page changes anywhere else; the 5,801 pages inherit it on the next build.
- **/industries — 101 chapters with no contents page; bare /industries 404s** [medium, measured]
  Problem: The chapter system is a lattice with no spine. 101 chapter pages receive 3,800+ inbound links (3 per company page, plus 14 from each grouping page), and the company pages' BreadcrumbList JSON-LD names the chapter as the middle crumb — but no page anywhere lists the chapters, and a reader who trims /industries/railroads to /industries gets a 404 (dist has no industries/index.html and _redirects has no /industries rule; it covers /articles, /archetypes, /businesses but not this).
  Evidence: ls dist/client/industries = 101 slug dirs, no index.html. grep of _redirects: zero /industries rules. grep across dist: pages linking more than one /industries/ slug = none (each grouping links only its own chapter; industries/railroads links zero other /industries/ pages). groupings/index.html links all 101 groupings and all 11 sectors but zero chapters.
  Proposal: Build /industries as a contents page — the 101 chapters listed under their 11 sector heads, exactly the data groupings/index already renders, framed as chapters rather than tables — and link it from each chapter's header. If the page is deferred, the two-line stopgap is a 301 in _redirects (/industries -> /groupings) so the trimmed URL lands on the nearest hub instead of a 404.
- **Company page, "Most recent quarterly filing" card (src/pages/c/[ticker].astro:293-313) -> the wire** [small, measured]
  Problem: Dead end from a company to the wire's record of its own filing. The card links only to EDGAR (external). The nav's /wire is a 543KB page with 919 company links and exactly one id ("main") — no per-item, per-ticker, or per-day anchors — so a reader chasing the entry must Ctrl-F through the whole tape. Meanwhile 116 per-day pages (/wire/YYYY-MM-DD) exist, chain backward day to day, and are linked from nowhere on the company page.
  Evidence: grep dist/client/c/ADM/index.html: the recent card's only anchor is the sec.gov URL; the page's two /wire links are the masthead and Portrait.astro:328's generic "the wire records it on arrival". grep dist/client/wire/index.html: 919 href=/c/ links, id set = {main, themeToggle, siteSearch...} only, 5 day-permalink links. ls dist/client/wire = 116 day directories.
  Proposal: In the recent-filing card, make the already-rendered date a link: "filed <a href=/wire/{recentFiling.date}>Jul 30</a>" (or append "the day's wire ->" beside "Source at SEC EDGAR ->"). recentFiling.date is already in scope and is exactly the day page's slug. One link lands the reader on a short page where their ticker sits among that day's filers.
- **Masthead nav (records cluster) and home page — the Manual** [small, measured]
  Problem: The Manual — three volumes, every company in fixed order, "entry N of 3,530" ribbons — is invisible until the reader has already found a company by other means. Home and the masthead carry zero links to it; its 3,819 inbound links all sit on company pages (2 each) plus one link buried in the 1.18MB /catalog page. A surface built for browsing cannot be browsed into.
  Evidence: grep dist/client/index.html for href="/manual": 0. grep -rl across dist: inbound = 3,613 /c pages + 204 /jp pages + catalog (1 link) + manual's own 54 pages. The nav's records cluster (BaseLayout.astro:390-395) is Catalog/Groupings/Almanac/Compare.
  Proposal: Add <a href="/manual">Manual</a> to the records cluster after Catalog. It is a records surface in the nav comment's own taxonomy. If an 11th masthead item is judged too wide (nav is flex-wrap, so it degrades gracefully at 700px), the fallback is a second browse door on home beside "101 comparative tables ->": "or read the Manual, page by page ->".
- **/flags (5 registers + index), /withheld, /tests — orphaned integrity surfaces** [small, measured]
  Problem: The flag registers have zero external inbound links in the entire built site — reachable only by typed URL or the legacy /archetypes/red-flags 301. /withheld (the honestly-withheld reads, the correctness campaign's proof of restraint) is linked from exactly one page, which is /flags itself — an orphan behind an orphan. /tests (the defensive workbook, Graham's named procedures) has one inbound page (almanac).
  Evidence: grep -rl across dist/client: href=/flags external inbound = 0 (6 self-links); href=/withheld inbound = flags/index.html only; href=/tests inbound = almanac (2 links) + self. _redirects comments confirm /flags and /tests are the sanctioned successors of the retired archetypes, i.e. surfaces the publication intends to keep.
  Proposal: Do NOT put Flags in the masthead — a top-level Flags item would give the registers verdict-like prominence (arrangement is a pronouncement). Instead: (a) the Almanac, which already links /tests/defensive twice, gets a sibling line linking /flags and /tests/net-nets — the almanac is where the cross-sectional registers naturally live; (b) /about and /docs/data each add one line linking /withheld beside their existing /ledger links, since withheld reads are the same honesty apparatus. Three one-line edits.
- **Industry chapter pages (src/pages/industries/[slug].astro) — leaves, not chapters** [medium, measured]
  Problem: Each chapter is a cul-de-sac: /industries/railroads links its grouping table (2x) and its members, but zero other chapters, no prev/next chapter, and — unlike the grouping page, which carries the full 11-sector strip — no link up to its sector page. Combined with the missing /industries index, a reader inside a chapter can only leave through a company or the table. The company page also never links its sector directly: sector is uniformly 2 clicks (company -> grouping -> sector strip).
  Evidence: grep dist/client/industries/railroads/index.html: href=/industries/* other than self = 0; href=/groupings/sector/* = 0. groupings/railroads/index.html by contrast links /groupings/sector/industrials 2x and all 11 sectors. c/ADM/index.html: zero /groupings/sector/ links.
  Proposal: Give each chapter the two links that make it a chapter: its sector's name in the header (to /groupings/sector/[slug]) and prev/next chapter within the sector at the foot — the exact pattern the company pages already ship for Manual order and Industry order (manFoot/indFoot). This plus the /industries contents page turns 101 leaves into a book.
- **/jp (204 pages) and /eu (45 pages) company pages — no sticky section bar; EU has no neighbor nav** [medium, measured]
  Problem: The orientation device the owner screenshotted — the sticky ticker bar with scroll-spied act tabs — exists only on /c/ pages. JP pages run 8 sections under act headers with no in-page nav at all; EU pages run 7 sections with no section bar, no Manual-order neighbors, no industry chapter links — their only wayfinding is "<- European catalog" (/catalog#europe).
  Evidence: grep -c secnav src/pages/jp/[ticker].astro = 0, src/pages/eu/[ticker].astro = 0; both have grep-counted id="sec-" sections (8 and 7). eu/[ticker].astro:94 shows the back link as the page's entire navigation apparatus; grep for Manual/industries in the EU page = 0 hits. The /c/ implementation (cbar + IntersectionObserver spy that self-prunes empty sections, [ticker].astro:268-279, 471-500) is self-contained and portable.
  Proposal: Lift the cbar/secnav block into a shared component and include it on jp and eu pages with their own act anchors. Secondary (separate decision): give EU pages the industry-chapter chip once EU names are classified — until then the section bar alone is the win.
- **Masthead labels "Owners" and "Statement" (BaseLayout.astro:404, 408)** [small, judgment]
  Problem: Two adjacent nav clusters are named "Owners" (/owners — the meetings and how the owners voted) and "Statement" (/owner — the reader's own look-through statement). The labels share a root, the URLs differ by one letter, and neither tells a first-time reader which page is about the companies and which is about themselves. "Statement" carries almost no scent at 12.5px uppercase.
  Evidence: Built titles: owners/index.html h1 = "The meetings, and how the owners voted"; owner/index.html h1 = "The owner's statement". They sit in consecutive nav clusters separated by one hairline, alongside "Account". This is the only pair in the nav where the destination cannot be predicted from the label.
  Proposal: Keep both URLs (published, cited). Relabel the nav items descriptively per the naming-restraint principle: "Owners" -> "Meetings" or "Votes" (the page's own h1 words), and/or "Statement" -> "My statement". Owner's call on the exact words; the finding is the measured ambiguity, the fix is a label, not a route.

Praise (must not break): 
- The global masthead is genuinely global and genuinely clustered: identical on every one of the 5,801 built pages (verified across 10 page types), organized as publication sections (records / publication / owners / reader) with hairline separators, plus site-wide "/" search and the "Open anywhere" uniform-random door that deliberately opens a comparative table, not a lone security. Do not flatten these clusters into a link list.
- The company page's sticky section bar is excellent and must not be touched in the retrofit: position:sticky top:0, four act tabs + Notes covering all 17 sections (every id="sec-*" nests inside an act), IntersectionObserver scroll-spy with accent underline, links self-prune when a section rendered empty, and the active tab auto-scrolls into view in the overflow row.
- The grouping page is a model hub: members across all four pools, 14 links to its own chapter, its sector page, the full 11-sector strip, and sibling groupings — from any company row, any peer is one click. The groupings index links all 101 tables and all 11 sectors.
- The dual prev/next system is labeled and legible: "Manual order: <- ADI · its page in the Manual · ADMA ->" (strict alphabetical by ticker within volume, one running entry number across the whole run) beside "Industry order: the Agricultural Products chapter · AGRO ->" (A-Z within chapter, cross-pool). Both repeat top and bottom. The orderings are pure arithmetic — no ranking smuggled in.
- Link-equity discipline: _redirects preserves every retired route with commented rationale (/articles, /archetypes, /businesses, renamed notes), and the 2026-07-25 section cuts were checked against published-note deep links before removal — sec-capalloc survives specifically because the-moat-and-the-multiple cites /c/ADBE#sec-capalloc. This is the binding pattern any nav change must follow.
- Progressive doors for the reader without a company in mind: the home "browse door" stands on load (its comment documents why), the noscript path points to the server-rendered /catalog, and old hash deep links (/#catalog, /#japan) forward with pool preserved.
- The wire's day pages chain backward (each day links the previous), and the home wire band gives every filer both its record page and the primary EDGAR source side by side — presentation without pronouncement, kept even at the top of the funnel.

### The Reader's Journeys

- **Journey 1, EPD leg — c:/Users/ryanr/.surplus-scout/ownerscorecard-terminal/dist/client/c/EPD/index.html Quality act (Graham dividend test, 'Where do the earnings go?' card, 'How the cash was used'); sources: src/lib/graham.mjs:115-124, scripts/fetchFundamentals.mjs:208, src/components/TenYear.astro:110** [medium, measured]
  Problem: The reader who lands on EPD asking 'is it any good' is told the cash-return story backwards: unread partnership distributions are rendered as the filed fact 'none paid' / '$0 dividends', and the page contradicts itself about it. EPD has raised its distribution 27 consecutive years (~$4.8B paid FY2025).
  Evidence: The built EPD page states: Graham test 'Uninterrupted dividends · none paid · Miss'; scorecard card 'Of $6.5B Owner Earnings, $300M (5%) went back to shareholders, $0 dividends, $300M buybacks' labeled 'Reinvests most of it'; sec-capital 'reads as a reinvestor... Retained (debt / cash) $30.9B · 45%' — while the SAME block hedges honestly: 'No dividend line was reported in the filing data over the span; the record here neither confirms nor rules out a payout', and the durability card contradicts both: 'this business returns cash through dividends and buybacks rather than reinvesting.' The record table has no Dividends-paid or Dividends/share row, and Retained-to-equity is cell-for-cell identical to ROE (11/12/17/19/16/18/21/20/21/20) — dividends=0 inside the formula. Cause measured: scripts/fetchFundamentals.mjs:208 reads dividendsPaid from only [PaymentsOfDividendsCommonStock, PaymentsOfDividends, PaymentsOfOrdinaryDividends]; MLPs tag distributions as DistributionsMade*/PaymentsOfDistributions*. graham.mjs:122 maps the all-null column to 'none paid' + fail, though its own comment (line 111) says 'A missing read is unknown'. Scale: 24 partnership-named pages carry '>none paid</b>' — ARLP BSM CQP DKL DMLP EPD ET GLP HESM KRP MNR MPLX NGL NRP PAA PAGP SGRY SPH SUN TXO UAN USAC WLKP XIFR — ET, MPLX, PAA, CQP, SUN, BSM, DKL, SPH among them active multi-billion payers (SGRY is a true non-payer corp; XIFR/NGL recently suspended). 710 pages total pair 'none paid' with the 'no dividend line reported' hedge; the rest are genuine non-payers where only the hedge inconsistency applies.
  Proposal: Two steps. (a) Honesty rule first, small: when the dividendsPaid column is null across the whole record, graham.mjs must render the test as unreadable ('no dividend line tagged in the data') and withhold pass/fail — it already has exactly this nuance for a single untagged year — and the 'Where do the earnings go?' card and TenYear summary must inherit the same hedge sec-capital already prints, never '$0 dividends'. (b) Widen the dividendsPaid tag family in fetchFundamentals.mjs to the partnership set (DistributionsMade, DistributionsMadeToLimitedPartnerCashDistributionsPaid, PaymentsOfDistributions, PaymentsOfCapitalDistribution), filled under the corroborated-fill ceremony already ruled for Brixmor (the blanket rung is refuted; corroborate per candidate against the filing's cash-flow statement).
- **Journey 3 — c:/Users/ryanr/.surplus-scout/ownerscorecard-terminal/dist/client/wire/index.html and /c/KO sec-recent; sources: src/lib/wirePerf.mjs, src/pages/c/[ticker].astro:310** [medium, measured]
  Problem: On the day's wire the biggest filer renders bare while a staffing microcap gets figures plus a company quote; clicking through to the filer's page, the head of 'The record' apologizes for figures that sit rendered three sections up on the same page.
  Evidence: Wed Jul 29 wire: 'KO Quarterly report 10-Q source →' with no delta line, while KFRC carries 'Revenue up 4.5%... Flex revenue for the six months... increased 2.3% to $666.1 million' plus the XBRL note. On /c/KO, act II opens with 'Most recent quarterly filing 10-Q filed Jul 29, 2026... No comparable figures were tagged in this filing's XBRL' — yet the vital signs on the same page show 'Latest quarter: Revenue $13.4B +6.7% vs yr-ago qtr; Operating margin 34.9% yr-ago 34.1%' and the record table carries a TTM Jul 2026 column, so the comparable figures exist in the site's own merged store. Counted: 49 of 3,613 built company pages render the apology banner; 45 of 288 10-Q items on the current wire page carry no delta line. Cut-safety checked: articles cite only #sec-record/#sec-oeq/#sec-capalloc/#sec-mix/#sec-peers/#sec-price (the-moat-and-the-multiple.mdx); #sec-recent is cited nowhere.
  Proposal: One extraction store for the journey: when wirePerf's comparable read fails, fall back to the filingFacts/companyfacts merged quarter that the vital-signs strip already reads (the same-evening merge shipped cf0d848) for both the wire one-liner and sec-recent; only if that too is empty, suppress the sec-recent banner entirely (subtraction, and it is article-safe) instead of printing an apology the page itself refutes.
- **Journey 2 — c:/Users/ryanr/.surplus-scout/ownerscorecard-terminal/dist/client/groupings/banks/index.html (all grouping tables, src/pages/groupings/[slug].astro)** [small, measured]
  Problem: On a 250-row table the only way into a company is the 3-5 character ticker link; the name cell — the widest, most-read cell — is an inert span, and there is no row-level navigation.
  Evidence: Row markup: <td class="txt tk"><a href="/c/JPM">JPM</a></td> beside <td class="txt nm"><span class="nmIn">JPMorgan Chase &amp; Co.</span></td>. The page scripts' listeners are sort buttons, the sector/industry selects, and compare-pick checkboxes only — no row click handler. Banks has 250 data rows; the journey is otherwise clean (3 clicks from home: Groupings → Banks → company).
  Proposal: Wrap the company name in the same /c/TICKER anchor as the ticker cell (keep the checkbox cell untouched so compare-picking never misfires). No visual redesign; the click target grows roughly tenfold on every one of the 101 tables.
- **Journey 1, KO leg — c:/Users/ryanr/.surplus-scout/ownerscorecard-terminal/dist/client/c/KO/index.html, price act toggle vs quality act** [small, measured]
  Problem: The same stock-compensation figure appears with two different values and two different roundings on one page, with no basis named — on a publication whose moat is correctness.
  Evidence: Price act toggle: 'Net of stock comp (−$266M)'. Everywhere else on the page: $279M (owner-earnings bridge row, bridge footnote 'less $279M... nearer $6.1B', buyback card, pay section). Buyback card: 'Stock compensation $279M, 0.6% of revenue'; pay section, same figure: '1% of revenue' ($279M / $47.9B = 0.58%). The $266M is presumably TTM while $279M is FY2025, but neither is labeled.
  Proposal: Name the basis wherever SBC is quoted ('FY2025' / 'TTM'), and adopt one rounding rule for sub-1% ratios (one decimal) so the two sections agree.
- **Journey 1 — KO acts II-III: sec-capalloc chart, 'Where do the earnings go?' scorecard card, sec-capital ten-year block; plus the twice-printed SBC doctrine paragraph** [small, judgment]
  Problem: Capital allocation is told three times, and the identical returned-vs-generated ratio is computed on two different windows (150% one-year on the card, 112% ten-year in sec-capital) without either naming its window against the other — the reader meets 150% then 112% and must reconcile them alone. The 'Buffett's oldest accounting fight' paragraph also prints verbatim in both the pay section and (as boilerplate) across pages.
  Evidence: KO: card — 'The company returned more than it generated: against $6.4B of Owner Earnings, $9.5B (150%) went back to shareholders, $8.8B dividends, $746M buybacks'; sec-capital — 'Returned to owners $89.7B, 112% of the owner earnings the business produced over the span, $72.9B as dividends and $16.9B as buybacks'; sec-capalloc — the per-year mix chart. The pay section's SBC paragraph on KO is word-for-word the paragraph on BSVN. Act III is 1,684 visible words on KO (details collapsed) of ~4,100 visible on the page. Cut-safety: #sec-capalloc is cited twice by the-moat-and-the-multiple.mdx and must stay; #sec-capital is uncited.
  Proposal: No cut. Make the card name its window ('this year: 150%; over the record: 112%, below') so the two figures read as one argument, and print the SBC doctrine paragraph once per page (keep it in the pay section, let the buyback card carry only the figures).
- **Journey 3 — the wire's 8-K 'Reported results' items (364 on the current page), src/pages/wire.astro** [medium, judgment]
  Problem: An earnings 8-K carries nothing but 'source →' offsite to EDGAR — the wire never says why it matters or when the figures will exist — while the same page's week-ahead already computes each filer's expected 10-Q window.
  Evidence: 364 'Reported results' items vs 288 10-Q items (243 with delta lines) on the saved Jul 31 wire page; every results-8-K row is bare ticker + type + offsite link. The week-ahead block computes 'expected, from each filer's own record: AMZN 7/31-8/3, AAPL 7/31-8/3...' for 120 names — the data to close the loop exists on the same page.
  Proposal: On a results 8-K, add the one line the site already knows: 'figures arrive with the 10-Q · expected 8/3-8/6' from the same window model (statutory due-by where no window closes early). Keeps the no-press-release-parsing line intact; the grave-item styling and alphabetical day order stay as they are.
- **Journey 1 — KO record table TTM column, c:/Users/ryanr/.surplus-scout/ownerscorecard-terminal/dist/client/c/KO/index.html** [medium, measured]
  Problem: Adjacent cells of one row mix tag bases: the TTM payables cell reads the 10-Q's combined payables-and-accrued figure against FY cells that read trade payables alone, flipping derived rows without annotation.
  Evidence: Accounts payable row: FY2025 $5.6B → TTM $15.4B (KO's 10-Q tags accounts payable and accrued expenses as one line); Operating working capital row consequently swings $1.8B (FY2025) → ($7.1B) (TTM) in neighboring cells; current ratio 1.5× → 1.3×. No footnote marks the basis change.
  Proposal: Pair-guard the quarterly payables tag basis the way the utilities reg A/L pair-guard works (if the Q tag is the combined concept, either decompose via the FY relationship or withhold the TTM cell for that row and its derivatives), or at minimum annotate the TTM cell with the tag it read.

Praise (must not break): 
- The four-act order holds on all three pages walked — The business (I) → The record (II) → Quality & stewardship (III) → The price (IV) is exactly Graham's rhythm (what is it → is it good → is it safe → what does it cost), the acts are numbered and nav-anchored, and the price correctly comes last and asks the reader to type it: records, never quotes, end to end.
- The collapse discipline is the page's best subtraction: 24 'What this means' blocks per page sit in <details class="why">, so KO's quality act reads at ~1,684 visible words instead of 2,631 raw — the student can open the doctrine, the scanner never wades through it.
- The groupings tables deliver every promise their intro copy makes, verified in the shipped CSS/JS: sticky header (position:sticky;top:0) AND sticky ticker/name columns, every column sorts both ways via real buttons, the sort order persists in the address bar, dashes and n/a sort to the back, and foreign filers without conversion terms show 'as filed: R$3.6B' and sort to the back rather than guessing a rate.
- Three clicks from home to a company (Groupings → Banks → /c/BSVN), with the breadcrumb, the Manual prev/next chain, and the Industry prev/next chain all closing the loop back out — no dead ends anywhere on the walk.
- The wire's week-ahead is the best page of the three journeys: expected windows drawn from each filer's own last four filings, statutory due-by dates otherwise, and the flat honesty of 'we do not estimate that date — nothing here is announced by the companies'; grave 8-Ks (auditor change, delisting, impairment) carry distinct styling and days with a single grave item still render (Jul 27, 24, 23).
- sec-recent with data is exactly right: ADT gets one line — 'Revenue up 2.0% year over year; operating income down 7.6%' — directly above the record table, with provenance ('figures computed from the filing's XBRL') and nothing more.
- The honest-withholding culture, where the data supports it, is the brand working: BSVN's charge-offs card reads 'Last reported FY2024... named, never passed off as current'; the banks bench marks medians resting on fewer than three years; the buyback block on EPD names the price paid per share and its range. The findings above are places where that same standard slipped, not where it is absent.
- BSVN's 'Next report: By 8/9 · due within 40 days of period end · has filed ~39 days after · the wire records it on arrival' is the single most terminal-like sentence on the site — the record teaching the reader when to come back.

### The Visual System

- **Typography scale — pool-wide, worst on the company page** [large, measured]
  Problem: Font sizes are accretion, not a scale. The pool holds 43 distinct font-size values (~760 declarations); a single built company page ships 33 distinct sizes — 24 fixed px values from 8.5px to 23px in half-pixel steps plus 9 clamp() variants. The 10.5–14px band alone contains 8 adjacent half-steps (10.5, 11, 11.5, 12, 12.5, 13, 13.5, 14) carrying 235 of the ~290 fixed-px declarations on that page. Half-pixel neighbors cannot be perceived as hierarchy; they read as the same size, slightly inconsistent from section to section.
  Evidence: grep census of src/components+src/pages+src/layouts: 98×13px, 98×12.5px, 86×12px, 76×11px, 69×13.5px, 68×11.5px, 66×14px, 45×10.5px … 43 distinct values. Shipped CSS for c/KO (dist/client/_astro/BaseLayout.Dn0Ym29z.css + _ticker_.DHz6LFAz.css + OwnerEarningsBridge.DlUWvQMr.css): 33 distinct font-size values.
  Proposal: Declare the working sizes as tokens in BaseLayout :root (~10 steps, e.g. --fs-micro:10.5px, --fs-label:11px, --fs-small:12px, --fs-note:13px, --fs-ui:14px, plus the existing body 17px and heading clamps) and migrate components opportunistically under the standing grant, collapsing half-step neighbors only where no deliberate distinction exists within one component family. Leave the deliberate ladder untouched: h1/act/secTitle clamps, 17px serif body, and everything inside the record table (owner's).
- **The uppercase kicker/label idiom — every component's small sans label** [medium, measured]
  Problem: The site's signature small uppercase sans label exists in at least 30 distinct size-by-tracking combinations (119 declarations; sizes 9.5–15px, letter-spacing 0.03–0.09em). 11px alone is paired with six different trackings (0.04 through 0.09em). Meanwhile the literal .kicker class is already standardized — 11px/700/0.09em uppercase — independently repeated in 7 page files. The standard exists; it was never promoted, so every new component re-invents it a half-step off.
  Evidence: grep of text-transform:uppercase blocks: 14×'11px 0.04em', 9×'11px 0.09em', 9×'10.5px 0.06em', 6×'12px 0.06em' … 30 combos. Identical .kicker rule at src/pages/flags/[register].astro:80, src/pages/concepts.astro:357, and 5 more files (two drift to 0.08em).
  Proposal: Promote the proven .kicker (11px/700/0.09em) to a global class in BaseLayout with one quieter sibling tier (10.5px/600/0.05em, the second-most-used cluster), then fold the near-misses (0.08em variants, orphan 11.5px cases) into them where the divergence encodes nothing. Keep genuinely distinct labels (the dateline, nav) as they are.
- **--faint as a text color — fails WCAG AA in both themes, on every page's footer** [small, measured]
  Problem: --faint measures 3.14:1 (dark, #67665f on #15161a) and 3.12:1 (light, #8b909a on #fcfcfa) against the AA 4.5:1 floor for normal text, and it is used as a real text color in 34 declarations plus the site footer's 12px legal text on all ~5,800 pages. Every other text tier passes (ink 15.1/17.1, ink-soft 11.5/13.1, ink-dim 7.6/10.4, muted 5.29/6.06; good/bad/warn 4.6–9.0).
  Evidence: Computed luminance ratios (node script) from the BaseLayout.astro:96–165 palettes. footer small { color: var(--faint); font-size: 12px } at src/layouts/BaseLayout.astro:306; text uses include DeliveredGrowth.astro:28,30 (.dl/.dn labels), compare/index.astro:143 (.rsub), index.astro:285 (.chipLbl), GroupingPicker.astro:146 (.ct).
  Proposal: Reclassify rather than brighten: move running text (footer small, .dl/.dn, .rsub, .chipLbl, .ct) to --muted, which passes in both themes, and reserve --faint for the incidental uses it already dominates — placeholders, dots, separators, and the deliberate em-dash of an absent cell (td.num.off), where de-emphasis of absence is the design. If any tier must stay textual-but-quieter than muted, raise --faint to #828177 dark / #6d737e light (4.61:1 / 4.64:1 measured).
- **.secHead — one class name, three treatments, one dead rule, and a flat outline** [small, measured]
  Problem: Three components define .secHead differently: Scorecard.astro:264 (11.5px uppercase sans with a bottom rule), MoatUnderAI.astro:102 (same size, no rule — and it renders inside the same scorecard card as a sibling subsection), and TenYear.astro:482 (serif clamp(22px,3.4vw,27px) — act-sized), which is dead CSS: its only occurrence in the file is the definition; the markup at TenYear.astro:414 uses .secTitle. Separately, the document outline is flatter than the visual one: on c/KO, the h3.secTitle 'Owner's Scorecard' is followed by seven h3.secHead subheads (six in Scorecard.astro:181,202,221 plus MoatUnderAI.astro:79) — eight same-level h3s for what reads visually as one section with seven subsections.
  Evidence: grep -c secHead src/components/TenYear.astro = 1 (definition only). KO heading census: 1×h1, 4×h2.actTitle, 13×h3.secTitle, 7×h3.secHead.
  Proposal: Delete TenYear's dead .secHead rule. Demote the scorecard/moat subheads to h4 (BaseLayout already styles h1–h4). Unify the subhead treatment between Scorecard and MoatUnderAI — the border difference may stand since .aimoat carries a border-top, but the two rules should say so rather than drift silently.
- **Vertical rhythm between company-page sections** [medium, measured]
  Problem: Section spacing is set per-component on 17 root elements and spans five values with no token: 18px (MoatUnderAI), 20px (Scorecard), 22px (TenYear, CapitalAllocation, Peers, RecordCharts), 24px (nine components), 26px (Drivers). The .sec wrappers in the page shell carry no rhythm at all (only scroll-margin-top), so the page's breathing is the accident of whichever component sits next to which.
  Evidence: Root-margin census: .scorecard margin:20px 0 (Scorecard.astro:246); .tenyear 22px, .ca 22px, .peers 22px, .rc 22px; .cpos/.debt/.mix/.brief/.val/.ask/.acq/.lease/.oeb 24px; .drivers 26px 0 0; .sec { scroll-margin-top: 52px } only, at src/pages/c/[ticker].astro:445.
  Proposal: One --sec-gap: 24px token applied by the page shell (.sec + .sec or .sec > :first-child margin-top), strip the root margins from the component sections, and let components keep their internal spacing freedom. The Scorecard's 20px and Drivers' 26px encode nothing an owner has ruled on; if MoatUnderAI's tighter 18px attach-to-card spacing is deliberate, it survives because it is internal to the scorecard card.
- **font-weight: 800 requested outside the loaded face's range** [small, measured]
  Problem: The Inter @font-face declares font-weight: 400 700, but 8 declarations request 800. Browsers clamp the variable axis to 700 (or apply synthetic bold, engine-dependent), so 800 never renders as designed and every intended 800-vs-700 distinction — a ticker heavier than its name, a total heavier than its rows — silently collapses or goes faux-bold inconsistently across browsers.
  Evidence: Face range at src/layouts/BaseLayout.astro:91. Requests: .letH in AdrCatalog.astro:84, EuCatalog.astro:85, JapanCatalog.astro:86, catalog.astro:152; EntryBlock.astro:75 (.eTicker); OwnerEarningsBridge.astro:235 (total amount); c/[ticker].astro:437 (.cbar-id); compare/index.astro:152 (.colTk). Weight census: 128×700, 105×600, 8×800, 1×650.
  Proposal: Change the eight 800s to 700 (what actually renders today, made honest), or extend the face declaration/axis if a true extra-heavy ticker weight is wanted. Either way the CSS should stop promising a weight the font does not ship.
- **Near-duplicate clamps and the .lede idiom** [small, measured]
  Problem: Sibling roles run on almost-identical formulas: the act title is clamp(22px,3.4vw,28px) while the global h2 is clamp(22px,3.4vw,27px) — 1px apart at max; the h1 is clamp(30px,5vw,42px) while Portrait's hero figure is clamp(30px,6vw,44px); the owners-area h1 is a fixed 26px on the index and a fixed 28px on the detail page. The lede/dek paragraph idiom repeats in at least 7 files with four different clamp values and five max-widths (66/68/70/72/78ch).
  Evidence: ActHeader.astro:20 vs BaseLayout.astro:201; Portrait.astro:344; owners/index.astro:115 (26px) vs owners/[ticker].astro:94 (28px); .lede at Capital.astro:195 and Inversion.astro:72 clamp(15,2.3vw,17), Valuation.astro:743 clamp(16,2.3vw,18.5), c/[ticker].astro:421 and wire.astro:181 and wire/[date].astro:112 clamp(16,2.4vw,19), compare/index.astro:95 clamp(16,2.3vw,19).
  Proposal: Pick one act/h2 clamp and one lede clamp (--fs-act, --fs-lede tokens beside the type-scale work), settle the owners h1 on a single value, and let max-width remain the one legitimately per-context knob. The Portrait hero figure outrunning the page h1 by 2px may be intent (the number is the hero); if so, one clamp with a comment saying so.
- **Stale var() fallbacks for --warn** [small, measured]
  Problem: Eight declarations write var(--warn, #b4690e), but #b4690e is a retired orange: the live --warn is #9a6a00 light / #dcb052 dark. The tokens are unconditionally defined in BaseLayout, so the fallbacks are dead weight today — and a wrong, theme-blind color if any component ever renders outside the layout.
  Evidence: Scorecard.astro:256,258; flags/index.astro:60,69; flags/[register].astro:80,82; account.astro:180. Hex census: #b4690e appears 8 times, all as fallbacks; the only other component-side hex is TenYear.astro:512's var(--good, #1a7a3c), which at least matches its token.
  Proposal: Drop the fallbacks (the tokens are guaranteed by the layout) or update them to the current light values. Subtraction preferred.
- **table.grp styles duplicated by hand across the two grouping surfaces** [medium, judgment]
  Problem: The industry table's frozen-pane CSS lives twice — 108 style-block lines in groupings/[slug].astro and 91 in groupings/sector/[slug].astro, 65 diff lines between them, mostly comments plus a few deliberate divergences. This is the same one-surface-drift shape the peer-bench completeness guard was built to kill, but at the CSS layer: a fix to the frozen-corner z-index or the sticky offsets lands on one surface and silently not the other.
  Evidence: diff of the two <style> blocks: 65 changed lines; the sector file itself says 'carried over verbatim from the industry table' and separately 'The industry reads darker here than on an industry table' — proof both of copying and of deliberate divergence living in the same block.
  Proposal: Do not homogenize now — the divergences are partly deliberate and only two surfaces exist. If a third grouping-shaped table ever ships, extract the frozen-pane mechanics (sticky offsets, z-index ladder, overscroll) into a shared component or stylesheet and keep the tonal differences local. Until then, a one-line comment in each file pointing at its twin is the cheap guard.

Praise (must not break): 
- The color system is real, not aspirational: 26 semantic tokens defined twice (light and dark) plus a forced-light print palette in BaseLayout.astro:96–165 and 332–341, and essentially zero raw hexes in components — everything found outside the layout was a var() fallback. This is the backbone any cleanup should extend, never replace.
- Tone semantics are consistent everywhere they appear: the scorecard's TONE_COLOR map (src/lib/fundamentals.mjs:168) resolves to the same --good/--bad/--warn variables the record table (TenYear td.pos/td.neg), Portrait's growth line, Valuation's spread, the wire's grave items, and the auth error states use. No component invents its own green or red.
- The bench and grouping tables' refusal to tone-color any cell is written doctrine — 'No row or cell is ever highlighted; no composite is ever formed' (groupings/[slug].astro:8) — and the audit confirms zero tone leakage into them. This divergence from the scorecard is the design; it must not be 'unified'.
- Contrast is strong where reading happens: body ink 15.1:1 dark / 17.1:1 light, and every primary text tier (ink, ink-soft, ink-dim, muted) at 5.29:1 or better in both themes; the tone colors run 4.6–9.0:1. Only --faint fails, and only where it strayed into text duty.
- The dark default is engineered, not painted: data-theme on the html element, a pre-paint localStorage script, a synced theme-color meta, color-scheme declarations for native form controls, a complete light palette behind the toggle, and a print override that forces light while keeping provenance on every sheet.
- The heading ladder holds down-page: act header (Roman numeral, 2px rule, clamp 22–28) → .secTitle (clamp 18–21) → 11.5px uppercase subheads — and the BaseLayout comment at line 204 records that .secTitle was created precisely to stop components rendering act-sized h2s. The system has self-corrected once already; the ladder is worth defending.
- The chrome discipline: the [hidden] !important guard, the :focus-visible ring, the skip link, masthead-anchored view transitions, and the print exhibit rules (thead repetition, break-inside guards, printed source URLs) all show a visual system built by subtraction and intent. The cleanup this audit proposes is consolidation of that voice, not a re-skin.

### Mobile and performance auditor (375px behavior, built-page weight, touch/keyboard affordances)

- **Company-page horizontal scrollers: TenYear .wrap, Peers .wrap, OwnerEarningsBridge .scroll, Valuation .sensWrap** [small, measured]
  Problem: The groupings tables ship overscroll-behavior-x: contain with a written rationale (a sideways swipe that reaches the pane's end is otherwise claimed by system back-navigation and the reader loses the page mid-read), but the four scrollers on every company page lack it. The record table opens pinned fully right, so the reader's very first gesture is a left-scroll toward that edge trap.
  Evidence: grep overscroll-behavior across src hits exactly two files: src/pages/groupings/[slug].astro:347 and src/pages/groupings/sector/[slug].astro:374. src/components/TenYear.astro:488 (.wrap { overflow-x: auto; }), src/components/Peers.astro:280, src/components/OwnerEarningsBridge.astro:208, src/components/Valuation.astro:796 have overflow-x: auto with no containment. The trap is named in the repo's own comment at groupings/[slug].astro:344-346.
  Proposal: Add overscroll-behavior-x: contain to the four declarations. One property, four files; the rationale is already written in-repo.
- **Catalog and owners-index filter inputs (iOS focus zoom)** [small, measured]
  Problem: iOS Safari zooms the whole page when a focused input's font-size is under 16px. The two page-local finders are the only inputs on the site below that line, so tapping the catalog search on a phone jolts the viewport; every other search field was already sized to avoid this.
  Evidence: src/pages/catalog.astro:140 .search font-size: 14px; src/pages/owners/index.astro:120 #ownersFilter font-size: 13.5px. Compare: compare/index.astro:104 #cmpInput 16px, SiteSearch.astro:65 #ssInput 17px, index.astro:247 #omni 17px.
  Proposal: Set both inputs to 16px (or add a max-width: 700px override to 16px if the desktop size is deliberate). Two declarations.
- **Company-page section tab bar (.secnav in src/pages/c/[ticker].astro, also used by /eu and /jp)** [small, measured]
  Problem: At 375px the bar overflows and scrolls, but the scrollbar is explicitly hidden and there is no fade, so nothing signals that the clipped last tab exists — and the last tab is Price, the page's destination section. Tab hit height is ~24px (11.5px type + 4px vertical padding), well under a 40px touch target.
  Evidence: src/pages/c/[ticker].astro:440-442: .secnav { overflow-x: auto; scrollbar-width: none } with ::-webkit-scrollbar { display: none }, tabs padding: 4px 9px at font-size 11.5px. Built MSFT page renders 4 tabs (The business / The record / Quality / Price); at 11.5px uppercase with 0.04em tracking the tabs plus the bold ticker compute to roughly 380-390px against 347px of content width at 375px (body padding 14px per side, BaseLayout.astro:308), so Price sits clipped. TenYear.astro:491 already implements the exact affordance (.fadeL edge gradient toggled by scroll position).
  Proposal: Container-level fix, not the record table's inner design: reuse TenYear's edge-fade pattern on .secnav (right-edge fade while scrollLeft < max), and raise tab padding to 4px 9px -> 10px 9px inside a max-width query so the bar's tap height reaches ~34-36px without changing desktop.
- **Small touch targets on real buttons: compare chip remove, Follow, Copy link, groupings compare-go, catalog A-Z bar** [small, measured]
  Problem: Every control is a genuine <button> (no divs-as-buttons anywhere — verified), but several sit at or under the WCAG 2.2 AA 24px floor and all sit far under a comfortable 40px: the compare chip's remove X is exactly 20x20, Follow computes to ~24px tall, Copy link and the groupings cmpGo pill ~23px, catalog A-Z letters ~22px squares, masthead nav links ~21px tall. The groupings sort buttons were deliberately bumped, but only to min-height 24px.
  Evidence: compare/index.astro:120 .chip .x { width: 20px; height: 20px }; FollowButton.astro:13-15 (12px type, 4px vertical padding); compare/index.astro:135 .copyLink padding 4px 12px; groupings/[slug].astro:333 .cmpGo padding 4px 12px; catalog.astro:146 .azbar a { padding: 3px 7px; min-width: 22px }; BaseLayout.astro nav a padding 3px 1px; groupings/[slug].astro:402-405 @media (pointer: coarse) { min-height: 24px; input 20x20 }.
  Proposal: Inside the existing @media (pointer: coarse) pattern (already established in groupings), lift these to >=40px hit areas via padding or a ::before hit-area extension — visual size can stay; the chip X specifically should become 28x28 minimum since it is destructive (removes a column).
- **Peers component (the peer bench on every company page) — the one wide table with no narrow-screen handling** [medium, measured]
  Problem: Peers.astro is the only consequential surface with zero @media and no frozen identity column: 8 nowrap columns scroll inside .wrap, and the company ticker column scrolls off-screen with them, so mid-scroll on a phone every row is an unlabeled line of figures. It also lacks TenYear's narrow font step and edge-fade. The other components without media queries degrade fine (RecordCharts SVGs are width:100% with preserveAspectRatio=none, Portrait uses clamp(), manual EntryBlock flex-wraps) — Peers is the gap.
  Evidence: Grep of every component: 18 components carry @media, Peers.astro is absent from the list; its CSS is .wrap { overflow-x: auto } with table white-space: nowrap (Peers.astro:280-281). Built MSFT peer table headers: Company / Revenue / Gross margin / Op. margin / ROIC / Owner earn. margin / Sales & marketing / Stock pay = 8 columns. Both sibling patterns exist in-repo: groupings' sticky-left identity cells (groupings/[slug].astro:363-369) and TenYear's sticky-right labels + 560px short-label step (TenYear.astro:497, 534-541).
  Proposal: Apply the in-house pattern: position: sticky on the company th (left, with opaque background and seam shadow exactly as groupings does), plus TenYear's 560px font step. No column is cut, nothing new is named.
- **Masthead nav on phones (every one of 5,801 pages)** [medium, judgment]
  Problem: The nav is 13 interactive items (search pill, 10 section links, Open anywhere, theme toggle) in a flex-wrap row; at 375px it wraps to roughly 3-4 lines, putting an estimated 130-170px of link chrome between the brand and the content on every page, with ~21px-tall link targets at 14px gaps. Nothing breaks — it just taxes every phone pageview twice (scroll distance and mis-tap risk).
  Evidence: BaseLayout.astro:382-414 markup (10 <a> in four navGroups + navSearch + navAside + themeToggle); .nav { flex-wrap: wrap } at :251; the only narrow handling is gap tightening and separator removal at 700px/520px (BaseLayout.astro:265-269, 308-311); nav a padding is 3px 1px at 12.5px type (:258-261).
  Proposal: Modest, arrangement-preserving: at <=520px give nav links 8px vertical padding (taller targets, similar height since lines are fewer than doubled), and let the least-used aside (Open anywhere) hide on narrow screens the way .navSearch kbd and .cbar-nm already do. Do not collapse into a hamburger — the sections-of-a-publication arrangement is deliberate.
- **Built-page weight of the list pages: /catalog 1.18MB, /groupings/sector/industrials 733KB, /manual/us/a 583KB, /wire 544KB, /owners 502KB (plus financials 590KB, IT 579KB, health-care 529KB, consumer-discretionary 504KB, manual/us/c 548KB)** [medium, measured]
  Problem: Ten built pages exceed 500KB of raw HTML. The named causes, measured per page: (a) Astro's per-element scope attribute is a third to nearly half of each page — catalog carries data-astro-cid-6kjp6l6a 16,912 times = 405,888 bytes (34.5% of 1,176,779), owners 224,448 bytes (44.9%), industrials 251,136 (34.3%), wire 160,104 (29.6%), manual/us/a 160,512 (27.6%); (b) catalog additionally carries 143,808 bytes of data-s attributes that duplicate the name+ticker already present as text in each of its 3,874 entries; (c) manual letter pages carry 218 inline sparkline SVGs = 82KB (legitimate content). Honest caveat: gzip crushes the repetition (catalog is 131KB over the wire, industrials 78KB), so this is parse-time, memory, and DOM-attribute cost on a phone, not transfer cost.
  Evidence: find dist/client -name '*.html' -printf sizes: top ten as listed; byte attribution computed by summing regex matches per file (cid attrs, data-s attrs, svg/script/style blocks); gzip -c | wc -c per file. Company pages are healthy: max 177KB (MSFT), median 137KB across 3,613; almanac 137KB; home 37KB.
  Proposal: For the five hot list pages only, move the list-item styles to <style is:global> keyed under a page-root class (e.g. .catalog .ent) so Astro stops stamping every element — this is exactly the scoping the CSS already implies, and drops ~1.2MB of raw bytes across the five worst pages. Drop catalog's data-s and have the filter script lowercase (ticker + ' ' + name) from textContent once at startup (3,874 strings, trivial). Leave the manual's sparklines alone — they are the content.
- **Dead build artifacts in dist/client/_astro** [small, measured]
  Problem: client.JUmeKr2y.js (185,988 bytes) and CompoundingExplorer.CZsivF1T.js are referenced by zero of the 5,801 built HTML pages — dead weight in the deploy (and a sign an island was removed but its import graph wasn't), though no reader ever downloads them.
  Evidence: grep -rl 'client.JUmeKr2y' --include='*.html' dist/client returns 0 files; same for CompoundingExplorer.
  Proposal: Find the orphaned import (likely a leftover client:load reference or an unused framework integration in astro.config) and remove it; confirm the 186KB bundle disappears from the next build.

Praise (must not break): 
- The record table's container behavior is the best mobile work on the site and must not be regressed: overflow-x wrap, opens pinned to the newest year with a left-edge fade signalling history, sticky-right row labels with a seam shadow, and a 560px step that swaps in short labels and tighter type (TenYear.astro:485-541). At 375px its ~700px-wide grid degrades exactly as designed.
- The groupings tables' phone treatment is genuinely engineered, with written rationale in the comments: frozen sel/ticker/name panes with exact offsets, pointer:coarse target enlargement, the <=700px header-wrap trade (pay the width cost once vertically in a sticky header, not on every column), 62vh pane height so a thumb can scroll past, and overscroll containment (groupings/[slug].astro:337-421). This is the pattern library the remaining fixes should copy from, not replace.
- The compare page's <=700px restructure — same DOM restyled into metric-major blocks with data-co labels so a four-column head-to-head never needs horizontal scroll (compare/index.astro:179-206) — is the strongest responsive idea on the site, and the innerHTML scope-attribute re-stamping comment shows it was debugged to work.
- Zero divs-as-buttons anywhere audited: sort controls are real <button>s inside <th scope='col'> with aria-sort maintained through JS (groupings/[slug].astro:244-245), the compare picker is a full ARIA combobox with ArrowUp/Down/Enter/Escape handling, checkboxes carry per-row aria-labels, and the skip link exists precisely so a keyboard reader isn't forced through the masthead 3,550 times (BaseLayout comment).
- Company pages — the product's center — are lean: 177KB max, 137KB median raw, ~36KB gzipped, 12 small deferred scripts, no framework runtime shipped to readers.
- The overlay search and home omnibox inputs are 17px, deliberately clear of iOS focus-zoom; only the two page-local finders missed the memo.

